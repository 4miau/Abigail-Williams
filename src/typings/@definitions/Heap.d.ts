import { CollectionConstructor } from '@discordjs/collection'
import { Collection } from 'discord.js'

/*
import { CollectionConstructor } from '@discordjs/collection'

interface Heap<K, V> extends Map<K, V> {
    constructor: CollectionConstructor;
}
*/